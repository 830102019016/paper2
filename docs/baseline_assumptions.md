# Baseline Assumptions and Module-Level Compliance Notes

> 本文件用于**集中说明 SATCON baseline 实现中的建模假设、模块合规性以及必要的简化说明**。  
> 其目的在于：  
> - 明确哪些模块**严格复现原论文设定**  
> - 哪些地方做了**合理简化**  
> - 提前消除 reviewer 可能提出的“实现不一致 / 假设不清晰”问题

---

## 1. User Distribution Module (`user_distribution.py`)

**状态：✅ 完全符合 SATCON 设定**

- 地面用户（MTs）均匀分布在半径 \(R\) 的圆形区域内
- 使用 \(r = R\sqrt{U}\) 的极坐标采样方式，确保二维均匀性
- 所有用户位于地面（\(z = 0\)）
- 信道条件分配为 50% SC（strong channel）与 50% WC（weak channel）

📌 本模块严格遵循 SATCON 仿真场景设定，无需额外说明或修正。

---

## 2. ABS Placement Module (`abs_placement.py`)

**状态：✅ 符合 SATCON（数值实现版）**

- 水平位置 \((x,y)\) 通过以下两种方法获得：
  - k-means（\(k=1\)）：几何质心
  - k-medoids（\(k=1\)）：距离和最小点
- 选择总平方距离（inertia）更小的方案
- ABS 高度通过数值搜索确定，使得**最远用户的 A2G 路径损耗最小**

说明：
> 原论文中高度可通过解析条件 \(\partial L^{A2G}(h,r)/\partial h = 0\) 获得，本实现采用等价的数值搜索方式，不影响优化目标与结论。

---

## 3. Channel Models Module (`channel_models.py`)

**状态：✅ 符合模型结构，简化假设已明确**

### 已实现内容
- 陆地移动卫星（LMS）信道采用简化 Loo 模型：
  - LoS 分量：对数正态分布
  - 多径分量：Rayleigh 分布
- 大尺度路径损耗采用自由空间路径损耗（FSL）模型

### 简化假设（已在代码中明确说明）
- 忽略多普勒效应与 Jakes 时变模型
- 每次 Monte Carlo 仿真采用独立信道实现
- 忽略空间相关性

✅ **代码验证**：
- 简化假设已在 `LooChannel` 类文档字符串中明确说明（channel_models.py:21-42）
- 包含了建议的论文说明语句

建议在论文中说明：
> *For simplicity, the Doppler effect is ignored and independent channel realizations are assumed in each Monte Carlo simulation.*

---

## 4. A2G / S2A Channel Module (`a2g_channel.py`)

**状态：✅ 模型正确，S2A 衰落假设已明确**

### A2G 链路
- 采用仰角相关路径损耗模型
- 包含 LoS 概率与 Rayleigh 小尺度衰落
- 信道增益归一化形式与论文一致

### S2A 链路（关键说明）
- 原 SATCON 公式中 **未包含小尺度衰落项**
- 本 baseline 实现中：
  - 默认忽略 S2A 小尺度衰落（等价于 fading gain = 1）
  - 仅考虑大尺度路径损耗

✅ **代码验证**：
- `S2AChannel` 类确实只实现了自由空间路径损耗（a2g_channel.py:195-213）
- `compute_channel_gain()` 中 fading_gain 参数在调用时设为 1.0
- 与论文 Eq.(7) 一致
- 假设已在类文档字符串中明确说明

建议在论文中明确：
> *Following Eq. (7) in SATCON, small-scale fading on the S2A link is ignored and only large-scale path loss is considered.*

---

## 5. Satellite NOMA Transmission Module (`noma_transmission.py`)

**状态：✅ 核心模型正确，SNR 定义表述已明确**

### 已实现内容
- 卫星侧采用最优 NOMA 用户配对（最弱 ↔ 最强）
- NOMA 功率分配与速率计算严格基于论文公式 (5) 与 (6)
- 每对用户占用 \(B_s / K\) 带宽

### 表述注意事项（已在代码中说明）
- 实现中使用 \( \text{SNR} \times \Gamma \) 的形式计算速率
- 需在论文中明确以下二选一表述：
  - \(\Gamma\) 已包含噪声归一化，SNR 为无量纲接收 SNR
  - 或 SNR 为等效发射 SNR，\(\Gamma\) 为纯信道增益

✅ **代码验证**：
- SNR 定义说明已在 `compute_channel_gains_with_pathloss()` 中明确（noma_transmission.py:55-76）
- 包含了建议的论文二选一表述
- 避免 reviewer 对 "功率 vs SNR" 定义产生歧义

---

## 6. NOMA Power Allocation Module (`power_allocation.py`)

**状态：✅ 公式正确，保护性机制已明确说明**

### 已实现内容
- 强用户功率分配因子 \(\beta_j\) 采用 SATCON 原论文公式 (4)
- 弱用户功率因子为 \(1 - \beta_j\)
- 用户配对策略引用经典 NOMA 最优配对方法

### 保护性机制说明（已在代码中明确）
- 在极端信道条件下，引入保守功率分配（弱用户 60%，强用户 40%）
- 该机制触发频率极低（<1%），不影响基线性能

✅ **代码验证**：
- 保护性机制已在 `compute_power_factors()` 中详细说明（power_allocation.py:30-91）
- 包含了建议的论文说明语句
- 明确了触发条件和影响范围

建议在论文中说明：
> *A safeguard power allocation is applied in rare degenerate cases to ensure the weak user always receives a higher power share.*

---

## 7. Utility Functions (`utils.py`)

**状态：✅ 工具层模块，不涉及系统模型假设**

- 单位转换、绘图、数据存储与性能指标计算
- 不影响系统建模与算法逻辑

---

## 总结（Reviewer 防火墙说明）

> The implemented baseline strictly follows the SATCON system model. All deviations from the original formulation are limited to clearly stated simplifications (e.g., Doppler-free channels, numerical ABS height search, and safeguard power allocation), which do not affect the qualitative conclusions of the baseline performance.

---

*建议将本文件与 `Baseline 修正计划.md`、`SATCON 决策逻辑总结.md` 一并保存，作为 baseline 实现与论文撰写的统一假设说明文档。*

