# SATCON Baseline 修正中的三个关键修正点

> 本文档专门整理 **在修正 SATCON baseline 实现时，必须立刻修正或澄清的三个关键技术点**。  
> 这三点并非方向性错误，而是**实现级与建模级的精修问题**；若处理不当，会导致：  
> - baseline 性能虚高或失真  
> - 与原论文逻辑不一致  
> - reviewer 指出“实现不严格”

---

## 修正点 1：ABS-OMA 的带宽分配方式（必须修改）

### ❌ 常见但错误的实现理解

```python
bandwidth_per_user = Bd / K   # ❌ 不严谨，容易误导
```

或在注释中写成：
> “K 对用户，每个用户半个时隙”

这**不符合 SATCON 原文设定**。

---

### ✅ 正确逻辑（SATCON 原文）

- 系统共有 **K 个卫星用户对**
- ABS 调度以 **“pair 为单位”**
- **每一对用户占用一个时隙 / 一个子带**：

\[
B_{\text{pair}} = \frac{B_d}{K}
\]

- 当 ABS 采用 **OMA** 且“仅转发某一用户 \(l\)”时：

> 👉 **整个 \(B_d / K\) 带宽 + 全部功率 \(P_d\)** 都分配给该用户

---

### ✅ 推荐实现方式

```python
bandwidth_per_pair = Bd / K
rates_oma[l] = bandwidth_per_pair * np.log2(1 + Pd * gamma_l)
```

📌 **不是** \(B_d / (2K)\)  
📌 **不是** “半个 pair 带宽”

---

## 修正点 2：ABS-NOMA 速率必须受 S2A 链路约束（DF 约束）

### ❌ 常见遗漏

在 ABS-NOMA 速率计算中，只考虑 A2G：

```python
R_dn_l = Bd/K * log2(1 + ... )  # ❌ 少了 S2A 约束
```

这在逻辑上等价于：
> ABS 是一个“无限回传能力”的理想节点

这**不符合 SATCON 的 Decode-and-Forward 设定**。

---

### ✅ 正确逻辑（SATCON 原文）

ABS 的 NOMA 转发速率定义为：

\[
R^{dn}_l = \min \left( R^d_l,\; R^{sd}_l \right)
\]

其中：
- \(R^d_l\)：ABS → 用户（A2G）速率
- \(R^{sd}_l\)：卫星 → ABS（S2A）解码速率

📌 **S2A 是 ABS 的信息瓶颈之一，不能忽略**。

---

### ✅ 推荐实现方式

```python
rate_a2g = bandwidth_per_pair * log2(...)
rate_s2a = s2a_rates[l]
R_dn[l] = min(rate_a2g, rate_s2a)
```

---

## 修正点 3：Hybrid 决策中的“对照速率”必须是卫星直达速率

### ❌ 潜在隐性错误

在 `hybrid_decision()` 中：

- 使用 A2G 速率作为对照
- 或遗漏 S2A / 卫星直达速率

会导致：
> ABS 被错误地判定为“更优路径”

---

### ✅ 正确对照关系（SATCON 原文含义）

对每个用户 \(l\)：

- **对照项**：
  \[
  R^s_l \quad \text{（卫星直达速率）}
  \]

- **候选项**：
  \[
  R^{dn}_l \quad \text{或} \quad R^{do}_l
  \]

ABS 是否服务用户的判据应是：

\[
R^s_l < R^{dn}_l \quad \text{或} \quad R^s_l < R^{do}_l
\]

📌 **不是 A2G vs 卫星**  
📌 **而是“端到端 ABS 路径 vs 卫星直达路径”**

---

### ✅ 推荐检查点

在 `hybrid_decision()` 中确认：

```python
# 正确对照
if sat_rate[l] < abs_rate[l]:
    serve_by_abs
else:
    serve_by_satellite
```

---

## 总结（给自己和 Reviewer 看的一句话版）

> The corrected baseline strictly follows the SATCON design: OMA transmission allocates the entire per-pair bandwidth to a single user, ABS-NOMA rates are constrained by the S2A decode-and-forward bottleneck, and all hybrid decisions are made by comparing end-to-end ABS rates against the direct satellite rates.

---

*本文件建议与《Baseline 修正计划.md》一并保存，用作实现检查清单与论文撰写依据。*